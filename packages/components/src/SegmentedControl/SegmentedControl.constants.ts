import type {
  SegmentedControlOrientation,
  SegmentedControlSize,
} from './SegmentedControl.types'

export const DEFAULT_ORIENTATION: SegmentedControlOrientation = 'horizontal'
export const DEFAULT_SIZE: SegmentedControlSize = 'md'
