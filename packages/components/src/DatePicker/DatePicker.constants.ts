/** Sunday-first week, the most common Western default. Range 0–6. */
export const DEFAULT_WEEK_STARTS_ON = 0

export const DEFAULT_FORMAT_OPTIONS: Intl.DateTimeFormatOptions = {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
}

/** Default popover offset below the trigger, in px. */
export const POPOVER_OFFSET = 6
