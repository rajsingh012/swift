import type { SkeletonAnimation, SkeletonVariant } from './Skeleton.types'

export const DEFAULT_VARIANT: SkeletonVariant = 'text'
export const DEFAULT_ANIMATION: SkeletonAnimation = 'pulse'
export const DEFAULT_LINES = 1
/** Fraction of full width used for the final text line (ragged paragraph edge). */
export const LAST_LINE_WIDTH = '60%'
