import type { Placement } from '../internal/floating'
import type { SelectSize, SelectState, SelectVariant } from './Select.types'

export const DEFAULT_PLACEMENT: Placement = 'bottom-start'
export const DEFAULT_OFFSET = 6
export const COLLISION_PADDING = 8
export const DEFAULT_SIZE: SelectSize = 'md'
export const DEFAULT_VARIANT: SelectVariant = 'outlined'
export const DEFAULT_STATE: SelectState = 'default'
